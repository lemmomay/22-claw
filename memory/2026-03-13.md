- Web Dashboard 常用域名：claw.maymay.de5.net（主机为 NAT，无公网；内网 IP 不可达对外）。

## 2026-03-13 工作记录

### Provider models 全量更新
- 标准工作流：① 拉取各 provider /v1/models → 写入 providers.<id>.models ② 同步 agents.defaults.models（按 providers 里实际模型重建，移除失效别名，值统一用 {}）③ 热重启 gateway。
- anyrouter 接口 403，暂时跳过。
- agents.defaults.models 结构是对象（dict），key 为 provider/model，value 为 {} 或含 alias 的对象，不是列表，修改时必须按原结构操作。
- OpenClaw 升级 2026.3.12 可用（稳定通道），当前运行版本 2026.3.2，暂未升级。

### SOUL.md 更新
- 用户发来中文版 SOUL.md（转发自小一 @xream），已按新规范重写 SOUL.md，保留名字「22」及与用户的关系设定不变。新规范要点：禁止列表结构、禁止复读预告、精炼短句、有人情味、禁止填充词。

### /alist 面板探索
- 目标：TG 里可点击管理 agents.list 子 agent 的默认模型（reading from agents.defaults.models），改完统一重启 gateway。
- skill/nativeSkills 方案验证失败：nativeSkills 只能把 skill 挂进 TG 命令菜单，不会原生拦截执行，/alist 仍会落回 main agent。
- 改 gateway 代码方案风险高，用户不熟悉，暂放弃。
- 临时方案（message 工具发按钮面板）太慢，用户不满意，放弃。
- 当前结论：web 端（claw.maymay.de5.net）无配置编辑 UI；agents.list 模型修改还是靠直接改 openclaw.json 或写本机交互脚本。
- 待续：用户同意做本机交互脚本方案（交互式选 agent + 选模型 + 备份写入）。
