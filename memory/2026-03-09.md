
## Image agent 规划

- 用户希望新增一个专门负责生图的 agent。
- 最初误以为模型来自 `elysiver/`，后来用户更正：应使用 `huan666/` 这组里的图像模型。
- 用户还明确表达：希望这个 image agent 不只绑定单一模型，后续还想把 `grok imagine` 系列也纳入可选范围。
- 当前较合理的方向：
  - 建一个 `image` agent
  - 默认主模型设为 `huan666/nano-banana-pro`
  - 后续把 `huan666/grok-imagine-*` 作为备选模型池

## 角色形象需求（长期可复用）

- 用户想生成一张“22”的可爱二次元看板娘图片。
- 核心视觉要求：
  - 可爱二次元少女
  - 看板娘气质
  - 戴一顶印有“22”字样的鸭舌帽
- 这可以作为 22 的视觉形象方向，后续适合作为头像、品牌角色、项目看板娘持续迭代。
